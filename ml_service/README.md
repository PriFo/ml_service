# ML Service 0.10.0

Production-grade ML Platform с GPU поддержкой, drift monitoring и real-time dashboard.

## 🆕 Что нового в версии 0.10.0

- ✨ **Асинхронные predict jobs** - предсказания теперь выполняются асинхронно с отслеживанием статуса
- ✨ **Универсальная система jobs** - единая система отслеживания для train, predict, drift задач
- ✨ **Мониторинг событий** - полное отслеживание всех операций с метаданными (IP, User-Agent)
- ✨ **Безопасность** - автоматическое выявление подозрительных запросов
- ✨ **EventDashboard** - детальный просмотр событий с input/output данными
- 🔄 **JobsTracker** - обновленный компонент для отслеживания всех типов задач

## 🎯 Особенности

- ✅ **MLPClassifier** с адаптивной архитектурой
- ✅ **GPU поддержка** (cuML, опционально)
- ✅ **Drift detection** (PSI + Jensen-Shannon divergence) с автоматическим сохранением baseline данных
- ✅ **Auto-retraining** при обнаружении drift
- ✅ **Real-time dashboard** (Next.js 15 + React 19)
- ✅ **CSS-first animations** (GPU-ускоренные)
- ✅ **Zero-dependency frontend** (только Next.js + React)
- ✅ **GDPR compliant** (cookie consent)
- ✅ **WebSocket** для real-time updates
- ✅ **Асинхронные задачи** - обучение и предсказания выполняются в фоне с отслеживанием статуса
- ✅ **Универсальная система jobs** - единая система отслеживания для train, predict, drift задач
- ✅ **Мониторинг событий** - полное отслеживание всех операций (drift, predict, train) с метаданными
- ✅ **Безопасность** - отслеживание IP адресов и User-Agent для выявления подозрительных запросов
- ✅ **Экспорт результатов** предсказаний в JSON/CSV
- ✅ **Логирование предсказаний** для drift detection
- ✅ **Графический интерфейс** для обучения, предсказаний и мониторинга

## 📁 Структура проекта

```
ml_service/
├── backend/
│   ├── ml_service/
│   │   ├── api/          # FastAPI endpoints
│   │   ├── core/         # Config, security, GPU detection, scheduler
│   │   ├── ml/           # ML engine, feature store, drift detector, validators
│   │   ├── db/           # Database models, repositories, migrations
│   │   └── tests/        # Unit tests
│   ├── ml_artifacts/     # Сохраненные модели, features, baselines
│   ├── ml_store.db       # SQLite база данных
│   └── requirements.txt
├── frontend/
│   ├── app/              # Next.js app directory
│   ├── components/       # React components (Dashboard, Predictor, ModelManager, etc.)
│   ├── lib/              # Store, API client, theme, consent
│   ├── styles/           # CSS (theme, animations, base)
│   └── package.json
├── datasets/             # Обучающие и тестовые датасеты
├── docker-compose.yml
├── START_UNIFIED.bat     # Унифицированный запуск (Windows)
├── start_unified.ps1     # PowerShell скрипт для Windows
├── run_backend.bat/sh    # Запуск только backend
├── run_frontend.bat/sh   # Запуск только frontend
└── README.md
```

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

**Windows:**
```bash
# Запустить все сервисы (backend + frontend) в одном окне
START_UNIFIED.bat

# Или по отдельности:
run_backend.bat    # Только backend (порт 8085)
run_frontend.bat   # Только frontend (порт 6565)
```

**Linux/Mac:**
```bash
# Сделать скрипты исполняемыми
chmod +x run_all.sh run_backend.sh run_frontend.sh

# Запустить все сервисы
./run_all.sh

# Или по отдельности:
./run_backend.sh    # Только backend (порт 8085)
./run_frontend.sh   # Только frontend (порт 6565)
```

### Вариант 2: Ручной запуск

**Backend:**
```bash
cd backend
pip install -r requirements.txt

# Создайте .env файл в корне проекта
# Скопируйте .env.example и отредактируйте
# Установите ML_ADMIN_API_TOKEN=your_secret_token_min_32_chars

# Запустите сервер
python -m ml_service
```

**Frontend:**
```bash
cd frontend
npm install

# Создайте .env.local (автоматически создастся при запуске)
# NEXT_PUBLIC_API_URL=http://localhost:8085
# NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8085/ws

npm run dev  # Запустится на порту 6565
```

### Вариант 3: Docker

```bash
docker-compose up --build
```

## 🧪 Тестирование

### Backend

```bash
cd backend
pytest
```

### Frontend

```bash
cd frontend
npm test
```

## 📊 API Endpoints

### Основные endpoints
- `POST /train` - Обучение модели (асинхронно, возвращает `job_id`)
- `POST /predict` - Создание задачи предсказания (асинхронно, возвращает `job_id`)
- `GET /predict/{job_id}` - Получение результатов задачи предсказания
- `POST /quality` - Проверка качества модели
- `GET /models` - Список всех моделей
- `GET /models/{model_key}` - Детальная информация о модели (все версии, метрики, параметры)

### Мониторинг и управление
- `GET /jobs` - Список всех задач (train, predict, drift, other) с фильтрацией по `model_key`, `status`, `job_type`
- `GET /jobs/{job_id}` - Статус конкретной задачи с детальной информацией
- `GET /events` - Список всех событий (drift, predict, train) с фильтрацией по `event_type`, `source`, `status`, `client_ip`
- `GET /events/{event_id}` - Детальная информация о событии с input/output данными
- `GET /events/suspicious` - Список подозрительных событий (множественные запросы с одного IP)
- `GET /events/by-ip/{ip}` - Все события с определенного IP адреса
- `POST /drift/check` - Ручная проверка drift (создает событие и job)
- `GET /health` - Проверка состояния сервиса (без аутентификации)
- `GET /health/alerts` - Активные алерты
- `GET /drift/daily-reports` - Отчеты о drift (с фильтрацией по model_key)
- `WS /ws` - WebSocket для real-time updates

### Документация API
- `GET /docs` - Swagger UI (требует аутентификации)
- `GET /redoc` - ReDoc документация (требует аутентификации)
- `GET /openapi.json` - OpenAPI схема (требует аутентификации)

## 💡 Примеры использования API

### Асинхронное предсказание

```bash
# 1. Создать задачу предсказания
curl -X POST "http://localhost:8085/predict" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "model_key": "nomenclature_label_classifier",
    "version": "1.0.0",
    "data": [
      {"field1": "value1", "field2": "value2"}
    ]
  }'

# Ответ:
# {
#   "job_id": "predict_a1b2c3d4",
#   "status": "queued",
#   "model_key": "nomenclature_label_classifier",
#   "version": "1.0.0",
#   "estimated_time": 5
# }

# 2. Проверить статус задачи
curl -X GET "http://localhost:8085/predict/predict_a1b2c3d4" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Ответ (во время обработки):
# {
#   "job_id": "predict_a1b2c3d4",
#   "status": "running"
# }

# Ответ (после завершения):
# {
#   "job_id": "predict_a1b2c3d4",
#   "status": "completed",
#   "predictions": [
#     {
#       "input": {"field1": "value1", "field2": "value2"},
#       "prediction": "label1",
#       "confidence": 0.95,
#       "all_scores": {"label1": 0.95, "label2": 0.05}
#     }
#   ],
#   "processing_time_ms": 150
# }
```

### Мониторинг событий

```bash
# Получить все события
curl -X GET "http://localhost:8085/events?limit=50" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить события определенного типа
curl -X GET "http://localhost:8085/events?event_type=predict&status=completed" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить подозрительные события
curl -X GET "http://localhost:8085/events/suspicious" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить все события с определенного IP
curl -X GET "http://localhost:8085/events/by-ip/192.168.1.100" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить детальную информацию о событии
curl -X GET "http://localhost:8085/events/{event_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Универсальные Jobs

```bash
# Получить все задачи
curl -X GET "http://localhost:8085/jobs?limit=50" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить задачи определенного типа
curl -X GET "http://localhost:8085/jobs?job_type=predict&status=running" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить статус конкретной задачи
curl -X GET "http://localhost:8085/jobs/{job_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🔧 Конфигурация

Создайте `.env` файл в корне проекта (или используйте `create_env.py`):

```bash
# Server
ML_SERVICE_HOST=0.0.0.0
ML_SERVICE_PORT=8085
ML_LOG_LEVEL=INFO

# Database
ML_DB_PATH=./ml_store.db
ML_DB_TIMEOUT=10

# Artifacts
ML_ARTIFACTS_ROOT=./ml_artifacts
ML_MODELS_PATH=./ml_artifacts/models
ML_FEATURES_PATH=./ml_artifacts/features
ML_BASELINES_PATH=./ml_artifacts/baselines

# Security
ML_ADMIN_API_TOKEN=your_secret_token_min_32_chars
ML_TOKEN_EXPIRY_DAYS=365

# Drift Detection
ML_DRIFT_PSI_THRESHOLD=0.1
ML_DRIFT_JS_THRESHOLD=0.2
ML_DAILY_DRIFT_CHECK_TIME=23:00
ML_CLIENT_DATA_CONFIDENCE_THRESHOLD=0.8
ML_RETRAINING_ACCURACY_DROP_THRESHOLD=-0.05

# ML Parameters
ML_HIDDEN_LAYER_SIZES=(512, 256, 128)
ML_LARGE_DATASET_HIDDEN_LAYERS=(1024, 512, 256, 128)
ML_LARGE_DATASET_THRESHOLD=500000
ML_ACTIVATION=relu
ML_SOLVER=adam
ML_MAX_ITER=500
ML_LEARNING_RATE_INIT=0.001
ML_ALPHA=0.0001
ML_EARLY_STOPPING=True
ML_VALIDATION_FRACTION=0.1
```

**Frontend** (`.env.local` в папке `frontend/`):
```bash
NEXT_PUBLIC_API_URL=http://localhost:8085
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8085/ws
```

## 🎨 Frontend Компоненты

- **Dashboard** - Главная панель с карточками состояния системы
- **Sidebar** - Боковое меню навигации
- **Login** - Авторизация пользователя
- **ModelManager** - Управление моделями (обучение, просмотр версий, параметры)
- **Predictor** - Предсказания с ручным вводом или загрузкой файла (CSV/JSON), асинхронная обработка с отслеживанием статуса, экспорт результатов
- **JobsTracker** - Универсальное отслеживание всех задач (train, predict, drift, other) с фильтрацией по типу и статусу
- **EventFeed** - Лента всех событий системы (drift, predict, train) с выделением подозрительных запросов
- **EventDashboard** - Детальная информация о событии с просмотром input/output данных, IP адреса и User-Agent
- **ServiceMonitor** - Мониторинг состояния сервиса
- **AdminPanel** - Панель администратора (Swagger/ReDoc)
- **ThemeDropdown** - Выбор темы оформления (light/dark/system)

## 📈 Метрики производительности

- **Bundle Size**: < 100 KB gzip (цель: 65 KB)
- **Lighthouse**: > 95 (цель: 98)
- **TTI**: < 1s (цель: 0.8s)
- **Response Time**: < 200ms (цель: 150ms)
- **Backend Port**: 8085
- **Frontend Port**: 6565

## 🔍 Ключевые возможности

### Drift Detection
- Автоматическое сохранение baseline данных при обучении модели
- Логирование всех предсказаний для сравнения с baseline
- Ежедневная проверка drift в 23:00 (настраивается) - создает системное событие
- Ручная проверка drift через API/GUI - создает событие и job
- Автоматическое создание алертов при обнаружении drift
- Поддержка PSI (Population Stability Index) и Jensen-Shannon divergence
- Отслеживание всех drift проверок как событий с метаданными

### Мониторинг событий
- **Универсальная система событий** - отслеживание всех операций (drift, predict, train)
- **Источники событий** - различение между API, GUI и системными событиями
- **Метаданные запросов** - автоматическое сохранение IP адреса и User-Agent
- **Безопасность** - выявление подозрительных запросов (множественные запросы с одного IP)
- **Детальная информация** - просмотр input/output данных для каждого события
- **Фильтрация** - поиск событий по типу, источнику, статусу, IP адресу

### Универсальная система Jobs
- **Единая система отслеживания** - все задачи (train, predict, drift, other) в одном месте
- **Типы задач** - разделение по `job_type`: train, predict, drift, other
- **Статусы** - queued, running, completed, failed
- **Этапы обработки** - детальное отслеживание прогресса (queued, loading_model, validating, predicting, training, completed)
- **Метаданные** - сохранение источника (API/GUI/system), IP адреса, User-Agent
- **Результаты** - сохранение результатов выполнения в поле metrics

### Предсказания
- **Асинхронная обработка** - каждое предсказание создает job с уникальным ID
- Поддержка ручного ввода данных (JSON)
- Загрузка файлов (CSV/JSON)
- Автоматическая валидация и нормализация данных
- Отслеживание статуса и этапов обработки (queued, loading_model, validating, predicting, completed)
- Автоматическое получение результатов после завершения
- Экспорт результатов в JSON/CSV с reference данными
- Логирование для drift detection
- Отслеживание IP адреса и User-Agent для безопасности

### Совместимость моделей
- Автоматическое определение формата модели (старый/новый)
- Поддержка старых моделей (один vectorizer для всех текстовых полей)
- Поддержка новых моделей (отдельный vectorizer для каждого поля)
- Автоматический fallback при несоответствии форматов

## 📝 Документация

Полная документация находится в папке `ТехническоеЗадание/`:
- `ML_Service_v3.2_TZ.md` - Полное техническое задание
- `QUICK_START.md` - Быстрый старт
- `frontend_css_first.md` - Руководство по фронтенду

## 🛠️ Разработка

### Backend

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск тестов
pytest

# Запуск с coverage
pytest --cov=ml_service --cov-report=html
```

### Frontend

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка
npm run build

# Тесты
npm test
```

## 📄 Лицензия

Proprietary

## 👥 Авторы

ML Service Development Team

