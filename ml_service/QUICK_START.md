# Быстрый старт ML Service 0.9.1

## 🚀 Запуск проекта

### Windows (PowerShell или CMD)

**Вариант 1: Двойной клик (самый простой)**
1. Откройте папку `ml_service`
2. Дважды кликните на `START.bat`
3. Готово! Сервисы запустятся автоматически

**Вариант 2: Через командную строку**
```powershell
cd ml_service
.\run_all.bat
```

**Вариант 3: Через PowerShell (если .\ не работает)**
```powershell
cd ml_service
& .\run_all.bat
```

### Linux/Mac

```bash
cd ml_service
chmod +x run_all.sh
./run_all.sh
```

## 📋 Что происходит при запуске

Скрипт автоматически:
1. ✅ Проверяет структуру проекта (исправляет если нужно)
2. ✅ Создает .env файл (если его нет)
3. ✅ Создает виртуальное окружение для backend
4. ✅ Устанавливает зависимости
5. ✅ Запускает backend на http://localhost:8085
6. ✅ Запускает frontend на http://localhost:6565

## ⚠️ Если возникли проблемы

### Ошибка "Имя не распознано"

В PowerShell используйте:
```powershell
.\run_all.bat
```

Или просто дважды кликните на `START.bat`

### Ошибка импорта ml_service

Убедитесь что папка переименована:
```powershell
cd ml_service\backend
# Должна быть папка ml_service (не ml_service_new)
dir
```

Если есть `ml_service_new`, переименуйте:
```powershell
Rename-Item ml_service_new ml_service
```

### Ошибка "модуль не найден"

Установите зависимости:
```powershell
cd ml_service\backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

## 🔧 Ручной запуск

Если скрипты не работают, можно запустить вручную:

### Backend
```powershell
cd ml_service\backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
python -m ml_service
```

### Frontend (в другом терминале)
```powershell
cd ml_service\frontend
npm install
npm run dev
```

## ✅ Проверка работы

После запуска откройте в браузере:
- Backend API: http://localhost:8085/health
- Frontend: http://localhost:6565

Должны увидеть ответ от сервисов.

## 📝 Примечания

- Backend и Frontend запускаются в отдельных окнах
- Для остановки просто закройте окна
- При первом запуске установка зависимостей может занять время
- .env файл создается автоматически при первом запуске

