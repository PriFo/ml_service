# ML Service 0.11.2

Production-grade ML Platform с GPU поддержкой, drift monitoring и real-time dashboard.

## 🆕 Что нового в версии 0.11.2

- ✨ **Система управления пользователями** - полное управление пользователями и их правами доступа
- ✨ **Профиль пользователя** - управление своим профилем, паролем и API токенами
- ✨ **API токены** - создание, управление и отзыв API токенов для программного доступа
- ✨ **Вкладка About** - информация о проекте, API документация и контакты разработчика
- ✨ **Улучшенная безопасность** - обработка системных токенов, оптимизация работы с БД
- 🔄 **Оптимизация БД** - улучшенная обработка блокировок, retry механизм с экспоненциальным backoff
- 🔄 **Исправления** - устранены ошибки с FOREIGN KEY constraints и блокировками БД

## 🎯 Особенности

- ✅ **MLPClassifier** с адаптивной архитектурой
- ✅ **GPU поддержка** (cuML, опционально)
- ✅ **Drift detection** (PSI + Jensen-Shannon divergence) с автоматическим сохранением baseline данных
- ✅ **Auto-retraining** при обнаружении drift
- ✅ **Real-time dashboard** (Next.js 15 + React 19)
- ✅ **CSS-first animations** (GPU-ускоренные)
- ✅ **Zero-dependency frontend** (только Next.js + React)
- ✅ **GDPR compliant** (cookie consent)
- ✅ **WebSocket** для real-time updates
- ✅ **Асинхронные задачи** - обучение и предсказания выполняются в фоне с отслеживанием статуса
- ✅ **Универсальная система jobs** - единая система отслеживания для train, predict, drift задач
- ✅ **Мониторинг событий** - полное отслеживание всех операций (drift, predict, train) с метаданными
- ✅ **Безопасность** - отслеживание IP адресов и User-Agent для выявления подозрительных запросов
- ✅ **Управление пользователями** - создание, редактирование, удаление пользователей (для администраторов)
- ✅ **Профиль пользователя** - управление паролем, именем пользователя и API токенами
- ✅ **Экспорт результатов** предсказаний в JSON/CSV
- ✅ **Логирование предсказаний** для drift detection
- ✅ **Графический интерфейс** для обучения, предсказаний и мониторинга

## 📁 Структура проекта

```
ml_service/
├── ml_service/
│   ├── backend/
│   │   ├── ml_service/
│   │   │   ├── api/          # FastAPI endpoints
│   │   │   ├── core/         # Config, security, GPU detection, scheduler
│   │   │   ├── ml/           # ML engine, feature store, drift detector, validators
│   │   │   ├── db/           # Database models, repositories, migrations
│   │   │   └── tests/        # Unit tests
│   │   ├── ml_artifacts/     # Сохраненные модели, features, baselines
│   │   ├── databases/        # Разделенные базы данных (models.db, users.db, logs.db)
│   │   └── requirements.txt
│   ├── frontend/
│   │   ├── app/              # Next.js app directory
│   │   ├── components/      # React components
│   │   ├── lib/              # Store, API client, theme, consent
│   │   ├── styles/           # CSS (theme, animations, base)
│   │   └── package.json
│   ├── datasets/             # Обучающие и тестовые датасеты
│   ├── STARTUP/              # Скрипты запуска сервисов
│   │   ├── START_ALL.bat      # Унифицированный запуск (Windows)
│   │   ├── start_all.ps1      # PowerShell скрипт
│   │   ├── start_all.sh       # Bash скрипт (Linux/Mac)
│   │   ├── run_backend.bat/sh
│   │   └── run_frontend.bat/sh
│   ├── help_scripts/         # Вспомогательные скрипты
│   │   ├── create_env.py     # Генерация .env файла
│   │   ├── generate_ssl_cert.bat/sh
│   │   └── check_status.bat
│   ├── AIText/               # AI-сгенерированные документы
│   │   └── *.md              # Анализы, summaries, документация
│   ├── git_helper/           # Скрипты для работы с Git
│   │   ├── do_commit.bat
│   │   └── COMMIT_MESSAGE.txt
│   ├── logs/                 # Логи работы сервисов
│   ├── docker-compose.yml
│   └── README.md
└── README.md                 # Этот файл
```

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

**Windows:**
```bash
# Запустить все сервисы (backend + frontend) в одном окне
cd ml_service/STARTUP
START_ALL.bat

# Или по отдельности:
run_backend.bat    # Только backend (порт 8085)
run_frontend.bat   # Только frontend (порт 6565)
```

**Linux/Mac:**
```bash
cd ml_service/STARTUP

# Сделать скрипты исполняемыми
chmod +x *.sh

# Запустить все сервисы
./start_all.sh

# Или по отдельности:
./run_backend.sh    # Только backend (порт 8085)
./run_frontend.sh   # Только frontend (порт 6565)
```

### Вариант 2: Ручной запуск

**Backend:**
```bash
cd ml_service/backend

# Создание виртуального окружения (если еще не создано)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла (если еще не создан)
# Используйте скрипт из help_scripts:
python ../help_scripts/create_env.py
# Или создайте вручную в корне ml_service/

# Запуск сервера
python -m ml_service
```

**Frontend:**
```bash
cd ml_service/frontend

# Установка зависимостей
npm install --legacy-peer-deps

# Создание .env.local (автоматически создастся при запуске)
# NEXT_PUBLIC_API_URL=http://localhost:8085
# NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8085/ws

# Запуск dev сервера
npm run dev  # Запустится на порту 6565
```

### Вариант 3: Docker

```bash
cd ml_service
docker-compose up --build
```

## 🧪 Тестирование

### Backend

```bash
cd ml_service/backend
pytest
```

### Frontend

```bash
cd ml_service/frontend
npm test
```

## 📊 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему (возвращает токен)
- `GET /auth/user-info` - Информация о текущем пользователе
- `GET /auth/profile` - Профиль текущего пользователя
- `PUT /auth/profile/password` - Изменение пароля
- `PUT /auth/profile/username` - Изменение имени пользователя
- `DELETE /auth/profile` - Удаление своего профиля

### Управление пользователями (только для администраторов)
- `GET /auth/users` - Список всех пользователей
- `GET /auth/users/{user_id}` - Информация о пользователе
- `POST /auth/users` - Создание нового пользователя
- `PUT /auth/users/{user_id}` - Обновление пользователя (tier, is_active)
- `DELETE /auth/users/{user_id}` - Удаление пользователя (soft delete)

### API Токены
- `POST /auth/tokens` - Создание нового API токена
- `GET /auth/tokens` - Список всех токенов пользователя
- `POST /auth/tokens/{token_id}/revoke` - Отзыв токена
- `DELETE /auth/tokens/{token_id}` - Удаление токена

### Основные endpoints
- `POST /train` - Обучение модели (асинхронно, возвращает `job_id`)
- `POST /predict` - Создание задачи предсказания (асинхронно, возвращает `job_id`)
- `GET /predict/{job_id}` - Получение результатов задачи предсказания
- `POST /quality` - Проверка качества модели
- `GET /models` - Список всех моделей
- `GET /models/{model_key}` - Детальная информация о модели

### Мониторинг и управление
- `GET /jobs` - Список всех задач (train, predict, drift, other)
- `GET /jobs/{job_id}` - Статус конкретной задачи
- `GET /events` - Список всех событий с фильтрацией
- `GET /events/{event_id}` - Детальная информация о событии
- `GET /events/suspicious` - Подозрительные события
- `GET /events/by-ip/{ip}` - События с определенного IP
- `POST /drift/check` - Ручная проверка drift
- `GET /health` - Проверка состояния сервиса
- `GET /health/alerts` - Активные алерты
- `GET /drift/daily-reports` - Отчеты о drift
- `WS /ws` - WebSocket для real-time updates

### Документация API
- `GET /docs` - Swagger UI (требует аутентификации)
- `GET /redoc` - ReDoc документация (требует аутентификации)
- `GET /openapi.json` - OpenAPI схема (требует аутентификации)

## 💡 Примеры использования API

### Аутентификация

```bash
# Регистрация
curl -X POST "http://localhost:8085/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "secure_password"
  }'

# Вход
curl -X POST "http://localhost:8085/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "secure_password"
  }'

# Использование токена
curl -X GET "http://localhost:8085/auth/user-info" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Создание API токена

```bash
curl -X POST "http://localhost:8085/auth/tokens" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My API Token"
  }'
```

### Асинхронное предсказание

```bash
# 1. Создать задачу предсказания
curl -X POST "http://localhost:8085/predict" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "model_key": "nomenclature_label_classifier",
    "version": "1.0.0",
    "data": [{"field1": "value1", "field2": "value2"}]
  }'

# 2. Проверить статус задачи
curl -X GET "http://localhost:8085/predict/{job_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🔧 Конфигурация

### Создание .env файла

Используйте скрипт из `help_scripts/`:
```bash
cd ml_service
python help_scripts/create_env.py
```

Или создайте вручную `.env` файл в корне `ml_service/`:

```bash
# Server
ML_SERVICE_HOST=0.0.0.0
ML_SERVICE_PORT=8085
ML_LOG_LEVEL=INFO

# Database
ML_DB_TIMEOUT=60
ML_DB_MODELS_PATH=./databases/models.db
ML_DB_USERS_PATH=./databases/users.db
ML_DB_LOGS_PATH=./databases/logs.db

# Artifacts
ML_ARTIFACTS_ROOT=./ml_artifacts
ML_MODELS_PATH=./ml_artifacts/models
ML_FEATURES_PATH=./ml_artifacts/features
ML_BASELINES_PATH=./ml_artifacts/baselines

# Security
ML_ADMIN_API_TOKEN=your_secret_token_min_32_chars
ML_TOKEN_EXPIRY_DAYS=365
ML_SESSION_EXPIRY_DAYS=30

# System Admin (создается при первом запуске)
ML_ADMIN_USERNAME=admin
ML_ADMIN_PASSWORD=admin

# SSL/HTTPS (опционально)
ML_USE_HTTPS=false
ML_SSL_CERT_FILE=./ssl/cert.pem
ML_SSL_KEY_FILE=./ssl/key.pem
ML_TRUST_PROXY=true

# Drift Detection
ML_DRIFT_PSI_THRESHOLD=0.1
ML_DRIFT_JS_THRESHOLD=0.2
ML_DAILY_DRIFT_CHECK_TIME=23:00
ML_CLIENT_DATA_CONFIDENCE_THRESHOLD=0.8
ML_RETRAINING_ACCURACY_DROP_THRESHOLD=-0.05

# ML Parameters
ML_HIDDEN_LAYER_SIZES=(512, 256, 128)
ML_LARGE_DATASET_HIDDEN_LAYERS=(1024, 512, 256, 128)
ML_LARGE_DATASET_THRESHOLD=500000
ML_ACTIVATION=relu
ML_SOLVER=adam
ML_MAX_ITER=5000
ML_BATCH_SIZE=auto
ML_LEARNING_RATE_INIT=0.001
ML_ALPHA=0.0001
ML_EARLY_STOPPING=True
ML_VALIDATION_FRACTION=0.1

# GPU & Resources
ML_MIN_WORKERS=1
ML_MAX_WORKERS_LIMIT=8
ML_WORKER_CPU_PER_TASK=0.5
```

**Frontend** (`.env.local` в папке `frontend/`):
```bash
NEXT_PUBLIC_API_URL=http://localhost:8085
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8085/ws
```

### Генерация SSL сертификатов

Для включения HTTPS используйте скрипт из `help_scripts/`:
```bash
# Windows
cd ml_service/help_scripts
generate_ssl_cert.bat

# Linux/Mac
cd ml_service/help_scripts
chmod +x generate_ssl_cert.sh
./generate_ssl_cert.sh
```

Затем установите в `.env`:
```bash
ML_USE_HTTPS=true
ML_SSL_CERT_FILE=./ssl/cert.pem
ML_SSL_KEY_FILE=./ssl/key.pem
```

## 🎨 Frontend Компоненты

- **Dashboard** - Главная панель с карточками состояния системы
- **Sidebar** - Боковое меню навигации
- **Login** - Авторизация пользователя
- **OverviewTab** - Обзор системы
- **ModelsTab** - Управление моделями
- **TrainingTab** - Обучение моделей
- **PredictTab** - Предсказания с асинхронной обработкой
- **JobsTab** - Универсальное отслеживание всех задач
- **EventsTab** - Лента всех событий системы
- **UsersTab** - Управление пользователями (только для администраторов)
- **ProfileTab** - Управление своим профилем
- **AboutTab** - Информация о проекте и API документация
- **AdminPanel** - Панель администратора (Swagger/ReDoc)
- **ThemeDropdown** - Выбор темы оформления

## 👥 Управление пользователями

### Роли пользователей

- **system_admin** - Полный доступ ко всем функциям, может управлять другими администраторами
- **admin** - Доступ к управлению пользователями и всем функциям системы
- **user** - Базовый доступ к обучению моделей и предсказаниям

### Первый запуск

При первом запуске автоматически создается системный администратор:
- **Username**: значение из `ML_ADMIN_USERNAME` (по умолчанию: `admin`)
- **Password**: значение из `ML_ADMIN_PASSWORD` (по умолчанию: `admin`)
- **Tier**: `system_admin`

⚠️ **Важно**: После первого запуска измените пароль администратора!

## 📈 Метрики производительности

- **Bundle Size**: < 100 KB gzip
- **Lighthouse**: > 95
- **TTI**: < 1s
- **Response Time**: < 200ms
- **Backend Port**: 8085
- **Frontend Port**: 6565
- **Concurrent Connections**: Поддерживает множество одновременных подключений (SQLite WAL mode, async FastAPI)

## 🔍 Ключевые возможности

### Управление пользователями
- Создание, редактирование и удаление пользователей (для администраторов)
- Управление правами доступа (tier: system_admin, admin, user)
- Активация/деактивация пользователей
- Защита от удаления последнего системного администратора
- Защита от удаления собственного аккаунта

### API Токены
- Создание именованных API токенов для программного доступа
- Просмотр всех токенов с информацией о создании и последнем использовании
- Отзыв токенов (деактивация без удаления)
- Удаление токенов
- Автоматическое истечение токенов (настраивается через `ML_TOKEN_EXPIRY_DAYS`)

### Drift Detection
- Автоматическое сохранение baseline данных при обучении модели
- Логирование всех предсказаний для сравнения с baseline
- Ежедневная проверка drift в 23:00 (настраивается)
- Ручная проверка drift через API/GUI
- Автоматическое создание алертов при обнаружении drift
- Поддержка PSI и Jensen-Shannon divergence

### Мониторинг событий
- Универсальная система событий - отслеживание всех операций
- Источники событий - различение между API, GUI и системными событиями
- Метаданные запросов - автоматическое сохранение IP адреса и User-Agent
- Безопасность - выявление подозрительных запросов
- Детальная информация - просмотр input/output данных

### Универсальная система Jobs
- Единая система отслеживания для всех типов задач
- Типы задач: train, predict, drift, other
- Статусы: queued, running, completed, failed
- Этапы обработки с детальным отслеживанием прогресса
- Метаданные: источник, IP адрес, User-Agent

## 🛠️ Разработка

### Backend

```bash
cd ml_service/backend

# Установка зависимостей
pip install -r requirements.txt

# Запуск тестов
pytest

# Запуск с coverage
pytest --cov=ml_service --cov-report=html
```

### Frontend

```bash
cd ml_service/frontend

# Установка зависимостей
npm install --legacy-peer-deps

# Запуск dev сервера
npm run dev

# Сборка
npm run build

# Тесты
npm test
```

## 📝 Документация

- **API Documentation**: Доступна через `/docs` (Swagger) и `/redoc` после аутентификации
- **About Tab**: Информация о проекте доступна во вкладке "About" в интерфейсе
- **AIText/**: AI-сгенерированные документы и анализы находятся в папке `ml_service/AIText/`

## 🔒 Безопасность

- Все API endpoints (кроме `/health` и `/auth/register`) требуют аутентификации
- Поддержка двух методов аутентификации:
  - **Session tokens** - создаются при логине через `/auth/login`
  - **API tokens** - создаются через `/auth/tokens` для программного доступа
- Системный токен из `.env` (`ML_ADMIN_API_TOKEN`) для обратной совместимости
- Автоматическое отслеживание IP адресов и User-Agent для выявления подозрительной активности
- Защита от SQL injection через параметризованные запросы
- Хеширование паролей (SHA256)
- Хеширование токенов перед сохранением в БД

## 📄 Лицензия

Proprietary

## 👥 Авторы

ML Service Development Team

## 📞 Контакты

Разработчик: [@pr1fo](https://t.me/pr1fo)
